# Firebase Project Configuration
# Note: Firebase Admin SDK uses Application Default Credentials when deployed
# For local development, set GOOGLE_APPLICATION_CREDENTIALS if needed

# Development Settings
NODE_ENV=development
